# Examples

No dossier example is included in the initial reference package. Add examples only when their provenance and metadata are available. Original Record files must remain local and must never be uploaded for verification.
